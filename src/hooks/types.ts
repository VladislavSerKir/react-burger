export type TInputValues = {
    name: string,
    email: string,
    password: string,
    token: string
}

export type TEventTarget = {
    target: {
        value: string,
        name: string
    }
}